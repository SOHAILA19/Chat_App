import 'package:flutter/material.dart';

class CalllIcon extends StatelessWidget {
  const CalllIcon({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.call,
      color: Colors.grey[900],
      size: 25,
    );
  }
}
